package pkg15070779drsappsystem;
import java.awt.*;
import javax.swing.*;

public class RecepScreen extends JFrame {
    public RecepScreen(){
        setDefaultCloseOperation (JFrame.EXIT_ON_CLOSE);
        setLayout (new BorderLayout());
        //add (makeReportsPanel (), BorderLayout.CENTER);
           setSize(800, 600);
    }
}
